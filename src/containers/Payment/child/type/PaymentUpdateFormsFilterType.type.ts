import { FormType } from '../../../../components/Forms/type/FormType.type';

export type PaymentUpdateFormsFilterType = {
  invoice: FormType;
  cost: FormType;
  status: FormType;
  notes: FormType;
};
